[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = src
markers =
    integration: integration tests that connect to live Azure Relay (long timeout)
    timeout: mark test with a timeout in seconds (requires pytest-timeout plugin)
